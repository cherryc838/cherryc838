let count=0;const countEl=document.getElementById("count"),toast=document.getElementById("toast"),cards=[...document.querySelectorAll(".product")];document.querySelectorAll(".add").forEach(b=>b.addEventListener("click",()=>{count++;countEl.textContent=count;toast.textContent=b.dataset.product+" added ✓";toast.classList.add("show");setTimeout(()=>toast.classList.remove("show"),1800)}));document.querySelectorAll("[data-filter]").forEach(b=>b.addEventListener("click",()=>{document.querySelectorAll("[data-filter]").forEach(x=>x.classList.remove("active"));b.classList.add("active");const f=b.dataset.filter;let n=0;cards.forEach(c=>{const show=f==="all"||c.dataset.category===f;c.style.display=show?"":"none";if(show)n++});document.getElementById("shown").textContent=n}));document.querySelector("[data-filter=all]").classList.add("active");document.getElementById("search").addEventListener("input",e=>{const q=e.target.value.toLowerCase();let n=0;cards.forEach(c=>{const show=c.dataset.name.includes(q)||c.dataset.category.includes(q);c.style.display=show?"":"none";if(show)n++});document.getElementById("shown").textContent=n});document.getElementById("newsletter").addEventListener("submit",e=>{e.preventDefault();e.currentTarget.querySelector("button").textContent="You're in ✓";e.currentTarget.querySelector("input").value=""});document.getElementById("year").textContent=new Date().getFullYear();